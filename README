docker build -f Dockerfile -t sozzler . --no-cache

docker run --rm --name sozzler -p3003:3000 -v`pwd`/src:/sozzler/src -v`pwd`/public:/sozzler/public -ti sozzler


TODO:
 - <title> :     "react-document-title": "^2.0.3",
 - truncate ingredient lists on RecipeList
 - ratings
   - original png olives
   - alignment
 - my-app => sozzler
 - better looking menu

